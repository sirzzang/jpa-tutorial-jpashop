spring: # 아래 설정 모두 없다고 하더라도 springboot는 기본적으로 memory mode
#  datasource:
#    url: jdbc:h2:mem:test # h2 database memory mode
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace # 쿼리 파라미터